name: Frontend Deploy

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Destino del despliegue"
        type: choice
        options:
          - staging
          - production
        default: production

permissions:
  contents: read
  actions: read

env:
  DEPLOY_PATH: ""
  COMPOSE_FILE: ""
  DEFAULT_COMPOSE_FILE: docker-compose.production.yml
  SSH_HOST: ""
  SSH_USER: ""
  SSH_KEY: ""
  SSH_PORT: ""

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment || 'production' }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Validate deployment secrets
        run: |
          echo "Validating deployment secrets..."
          MISSING_SECRETS=()
          
          if [ -z "${{ secrets.SSH_HOST }}" ]; then
            MISSING_SECRETS+=("SSH_HOST")
          fi
          if [ -z "${{ secrets.SSH_USER }}" ]; then
            MISSING_SECRETS+=("SSH_USER")
          fi
          if [ -z "${{ secrets.SSH_KEY }}" ]; then
            MISSING_SECRETS+=("SSH_KEY")
          fi
          if [ -z "${{ secrets.DEPLOY_PATH }}" ]; then
            MISSING_SECRETS+=("DEPLOY_PATH")
          fi
          
          if [ ${#MISSING_SECRETS[@]} -ne 0 ]; then
            echo "❌ Error: The following required secrets are not configured:"
            for MISSING in "${MISSING_SECRETS[@]}"; do
              echo "  - $MISSING"
            done
            echo ""
            echo "📝 Required secrets configuration:"
            echo "  - SSH_HOST: IP or domain of your VPS"
            echo "  - SSH_USER: SSH username (e.g., csadmin)"
            echo "  - SSH_KEY: Complete SSH private key"
            echo "  - DEPLOY_PATH: /srv/codeqlick/chatroulette/frontend"
            echo ""
            echo "Please configure them in GitHub Settings > Secrets and variables > Actions > Environment: production"
            exit 1
          fi
          
          # Validate DEPLOY_PATH format
          DEPLOY_PATH="${{ secrets.DEPLOY_PATH }}"
          if [[ ! "$DEPLOY_PATH" =~ ^/ ]]; then
            echo "❌ Error: DEPLOY_PATH must be an absolute path (starting with /)"
            echo "Current value: $DEPLOY_PATH"
            echo "Expected: /srv/codeqlick/chatroulette/frontend"
            exit 1
          fi
          
          echo "✅ All required secrets are configured."
          echo "📂 Deploy path: $DEPLOY_PATH"

      - name: Prepare deployment env
        shell: bash
        run: |
          {
            echo "DEPLOY_PATH=${{ secrets.DEPLOY_PATH }}";
            echo "COMPOSE_FILE=${{ secrets.COMPOSE_FILE }}";
            echo "DEFAULT_COMPOSE_FILE=docker-compose.production.yml";
            echo "SSH_HOST=${{ secrets.SSH_HOST }}";
            echo "SSH_USER=${{ secrets.SSH_USER }}";
            echo "SSH_PORT=${{ secrets.SSH_PORT }}";
            echo "SSH_KEY<<'EOF'";
            echo "${{ secrets.SSH_KEY }}";
            echo "EOF";
          } >> "$GITHUB_ENV"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: dist
          if-no-files-found: error

      - name: Deploy to OVH via SSH
        uses: appleboy/ssh-action@v1.2.0
        env:
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
          COMPOSE_FILE: ${{ secrets.COMPOSE_FILE }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          host: ${{ env.SSH_HOST }}
          username: ${{ env.SSH_USER }}
          key: ${{ env.SSH_KEY }}
          port: ${{ env.SSH_PORT || 22 }}
          envs: DEPLOY_PATH,COMPOSE_FILE,GITHUB_TOKEN
          script: |
            set -euo pipefail
            DEFAULT_COMPOSE_FILE="docker-compose.production.yml"
            
            # Validate DEPLOY_PATH
            if [ -z "${DEPLOY_PATH:-}" ]; then
              echo "❌ Error: DEPLOY_PATH is not set or is empty"
              exit 1
            fi
            
            echo "📂 Deploy path: $DEPLOY_PATH"
            
            # Calculate project root (parent of frontend directory)
            PROJECT_ROOT="$(dirname "$DEPLOY_PATH")"
            echo "📁 Project root: $PROJECT_ROOT"
            
            # Create project root directory if it doesn't exist
            if [ ! -d "$PROJECT_ROOT" ]; then
              echo "⚠️  Project root directory $PROJECT_ROOT does not exist. Creating it..."
              mkdir -p "$PROJECT_ROOT"
            fi
            
            # Clone repository if it doesn't exist, or update if it does
            cd "$PROJECT_ROOT" || {
              echo "❌ Error: Failed to change to project root $PROJECT_ROOT"
              exit 1
            }
            
            # Check if this is a valid git repository
            if [ -d ".git" ] && git rev-parse --git-dir > /dev/null 2>&1; then
              echo "📥 Valid git repository found. Pulling latest code from main branch..."
              # Configure git remote if not exists
              if ! git remote get-url origin > /dev/null 2>&1; then
                git remote add origin "https://github.com/${{ github.repository }}.git"
              fi
              # Update remote URL with token if available
              if [ -n "${GITHUB_TOKEN:-}" ]; then
                git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git"
              fi
              git fetch origin main
              git reset --hard origin/main
              git clean -fd
            else
              echo "📥 Repository not found or invalid. Setting up repository..."
              
              # If directory exists but is not a git repo, initialize git in place
              if [ "$(ls -A . 2>/dev/null)" ]; then
                echo "⚠️  Directory exists but is not a git repository. Initializing git in place..."
                git init
                git remote add origin "https://github.com/${{ github.repository }}.git" 2>/dev/null || {
                  git remote set-url origin "https://github.com/${{ github.repository }}.git"
                }
                if [ -n "${GITHUB_TOKEN:-}" ]; then
                  git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git"
                fi
                git fetch origin main
                git reset --hard origin/main
                git clean -fd
                echo "✅ Repository initialized and updated"
              else
                # Directory is empty, clone fresh
                echo "📥 Cloning repository into empty directory..."
                REPO_URL="https://github.com/${{ github.repository }}.git"
                if [ -n "${GITHUB_TOKEN:-}" ]; then
                  REPO_URL="https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git"
                fi
                git clone --branch main --single-branch --depth 1 "$REPO_URL" .
                echo "✅ Repository cloned successfully"
              fi
            fi
            
            # Verify docker-compose.production.yml exists in project root
            COMPOSE_PATH="${COMPOSE_FILE:-$DEFAULT_COMPOSE_FILE}"
            if [ ! -f "$COMPOSE_PATH" ]; then
              echo "❌ Error: $COMPOSE_PATH not found in $PROJECT_ROOT"
              echo "📋 Available files in project root:"
              ls -la || true
              exit 1
            fi
            
            echo "✅ Found $COMPOSE_PATH in project root"
            
            # Check if .env.production exists, create if it doesn't
            if [ ! -f ".env.production" ]; then
              echo "⚠️  .env.production not found. Creating from template..."
              {
                echo "# Variables de entorno para producción"
                echo "# IMPORTANTE: Actualizar estos valores con los secrets reales"
                echo ""
                echo "# PostgreSQL"
                echo "POSTGRES_USER=codeqlick"
                echo "POSTGRES_PASSWORD=CHANGE_ME_POSTGRES_PASSWORD"
                echo "POSTGRES_DB=codeqlick"
                echo "POSTGRES_HOST=postgres"
                echo "POSTGRES_PORT=5432"
                echo ""
                echo "# Redis"
                echo "REDIS_PASSWORD=CHANGE_ME_REDIS_PASSWORD"
                echo "REDIS_HOST=redis"
                echo "REDIS_PORT=6379"
                echo ""
                echo "# Frontend"
                echo "VITE_API_URL=https://api.codeqlick.com/api/v1"
                echo "VITE_WS_URL=wss://api.codeqlick.com"
              } > .env.production
              echo "⚠️  Created .env.production with default values. Please update with real secrets!"
              echo "⚠️  You should configure this file manually on the VPS with the correct production values."
            else
              echo "✅ Found .env.production"
            fi
            
            # Deploy frontend
            echo "🔨 Building and deploying frontend..."
            docker-compose -f "$COMPOSE_PATH" pull frontend || true
            docker-compose -f "$COMPOSE_PATH" build --no-cache frontend
            docker-compose -f "$COMPOSE_PATH" up -d frontend
            
            echo "✅ Frontend deployment completed"
            docker-compose -f "$COMPOSE_PATH" ps

